{
  "name": "encounter-837p-platform",
  "version": "0.1.0",
  "private": true,
  "description": "Starter platform for deterministic 837 encounter validation",
  "type": "module",
  "scripts": {
    "start": "python3 -m http.server 4173",
    "test": "node --test tests/*.test.js",
    "check": "node --check ui/app.js && node --check rules/validation.js && node --check agent/advisor.js"
  },
  "engines": {
    "node": ">=20"
  }
}
